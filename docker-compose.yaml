services:
  postgres:
    image: postgres:latest
    container_name: ecom-postgres
    environment:
      POSTGRES_DB: ecom
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - /home/namplh/docker-data:/var/lib/postgresql

volumes:
  postgres_data: